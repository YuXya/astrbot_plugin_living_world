# 更新记录

## [0.2.6] - 2026-09-11

- 删除发送限制面板、每日发送上限和独立免打扰，主动聊天与群聊插话统一使用日程起止范围。同对象冷却移至生成与细化设置，保留已有值、0 可关闭；与日程参数共同轻量保存，不触发维护或全站刷新。

- 日程新增自定义开始／结束时间，默认 08:00—24:00；允许空档但首尾覆盖，统一校验范围、活动数和重叠。参数按日冻结，轻量保存默认次日生效，重生成成功才替换今天。范围外显示睡梦中并停止主动聊天／插话，生成排队和发送锁等待后重新检查；普通回复保留，不创建睡眠活动或迁移旧日程。

- 记忆卡片按参考画像页紧凑展示星标、强度、实际时间、完整结论、事实依据和全部标签；点击从右侧滑出归属、场合、保留状态、版本与编辑操作。历史及来源继续按需读取，保存局部更新、关闭恢复列表位置。

- 记忆提炼改为持久化批次、逐条事务保存与最多一次自动重试；概括依据使用 source_ids 校验，修复仅处理失败候选，完成清理临时正文。
- 新增分页提炼进度、按需详情及手动重试；调用记录区分模型与保存结果。精简提炼默认勾选与重复材料，补齐六组 JSON 案例；不迁移现有配置和数据。


## [0.2.5] - 2026-09-09

- 2026-09-11：移除上下文包装、记忆条目、日程资料和当前默认写作模板中反复强调角色日常虚构及计划未发生的说明；保留活动实际状态与场合隔离。补充普通聊天历史上限所在的 AstrBot 配置入口，明确它与记忆提炼批次、群观察窗口的区别。仅静态检查与打包，不修改宿主配置或运行中的数据。

- 2026-09-11：重写「记忆与画像」，默认多人物卡片首页，进入档案按五属性或发生时间浏览，保留全库搜索。直接展示完整结论、依据、标签、实际时间、来源场合与保留状态；来源及版本差异按需读取。首页只读摘要，昵称异步补全，全局状态不再携带完整记忆库；保存、标记、删除局部更新并保留编辑草稿与版本冲突信息。保持现有内容、归属及记忆业务，不迁移数据、不恢复模型试跑。本次仅静态检查与打包语法校验，由用户自行安装和发布。

- 2026-09-11：删除模型试跑页面、编辑器、草稿、记录复制入口、三个管理接口及后台执行代码。上下文排序中原「模型试跑」前缀改为「提示词模板」，共用业务资料继续保留原标识及排序勾选；调用记录及模板编辑保留。仅做静态检查，未运行回归或在线联调。

- 2026-09-11 最新调整：以当前数据格式为基准，删除日程、内在状态、旧记忆、上下文布局及用量的升级转换和旧备份转换；当前默认值直接声明，保留已保存的当前设置。删除旧资料迁移入口，正常提炼队列继续运行，备份格式改为 v2，旧备份拒绝导入。
- 调试瘦身：普通快照不再复制宿主全部聊天历史，媒体 Base64 只留说明；实际 HTTP 原文保持。页面全量状态只含调试列表，选择轮次时才读取该轮正文；调试保留清理使用缓存元数据。空搜索不扫描全文，收起的完整记录展开后才生成正文，查看记忆变更不重绘整个页面。
- 按用户要求停止回归测试。上述最后调整仅做静态语法检查和清理范围预览，以下此前的测试数字不代表最新调整已完成回归。


- 2026-09-11：进一步修复保存／恢复默认模板、活动 JSON 批量保存、单条活动及角色状态保存、查看记忆变更记录后的全量刷新等待；只更新保存结果及相关日程档案。回复页面保留插话间隔、只改文案时也走轻量保存。失败保留草稿，JSON 复制和下载继续在浏览器完成。
- 2026-09-11：人物画像列表及详情显示「昵称（QQ 号码）」，优先私聊白名单手填称呼，否则按来源连接只读获取 QQ 昵称，陌生群成员可回退成员信息中的昵称；成功缓存一小时、失败一分钟，整页昵称查询最多等待两秒。失败显示“未获取昵称”，不重复显示 `QQ qq:`；不修改记忆归属、正文或历史迁移数据。

- 2026-09-11：修复「保存上下文用量」卡顿。上下文用量、排序及单独的回复文案保存只校验并持久化配置，不运行日程检查、记忆维护、迁移或调试清理；页面直接采用保存接口返回的配置，不再等待全部档案、调用记录和会话诊断刷新。保存失败保留草稿，在途请求继续沿用原快照。

- 2026-09-11：统一为五属性画像记忆，按人格名称保存自身档案、按 QQ 身份保存人物档案；聊天默认 5 轮或闲置 10 分钟后台提炼，经历、上网摘要、日记和笔记即时入队，持久批次可重启续跑。普通模型扩词配合 BM25 召回，另提供跨日近期记忆及主动记忆／召回工具；版本修正、合并、有用性反馈、默认关闭的三档遗忘与彻底删除保护均可管理。取消旧独立记忆分类与日记／笔记简报，保留来源正文及事实摘要／感想。
- 2026-09-11：布局升级到版本 4、用量升级到版本 2，保留全局顺序、默认基准及各任务勾选，新增「记忆与画像」「近期记忆」两个入口。旧数据先归档再分批整理，保留时间、场合和身份；快照、备份转换及旧调用解释兼容，无 SQLite 表结构迁移。最终 678 项 Python 测试通过、12 项兼容测试跳过；Ruff、JavaScript、完整 Edge 桌面冒烟及本地 HTTP 对照通过。在线模型和真实 QQ 联调另记。

- 2026-09-11：上下文布局升级到版本 3，全部任务显示相同完整资料列表，一套全局顺序及 system／user 位置配合各任务独立勾选；已有全局顺序成为默认基准，旧任务独立排序归档。正式调用、组合试跑和工具后调用沿用本轮资料与选择快照，未勾选记忆不强化、不影响已选资料去重；备份携带全局顺序、基准及任务勾选，旧快照按原版解释，无 SQLite 表结构迁移。
- 2026-09-11：交谈对象改为目标名字／群聊名字，优先手填称呼，否则只读对应 QQ 连接的昵称／群名；成功缓存一小时、失败一分钟、读取超时两秒，失败显示“未设置名字”。群聊、私聊及主动聊天默认使用三条完整内容的简版日程，可切换完整日程或取消；分别维护三份回复要求，首次复制原群聊文案后独立保存，均支持留空及恢复默认。当前阶段想法默认只在活动细化勾选，也可供其他任务选择，仍不提供数值机制。下列较早记录保留当时实现与验收事实。

- 2026-09-10：上下文名称统一为「实际二级菜单：具体资料」，贯通排序、用量、分类、来源、新调用①及模型标题。记忆拆成七类独立额度，四类见闻合并取最新、天气独立；十类初始合计 28 条只读，日记／笔记各自设置字符上限。用量移入 02，每行带来源入口。主动聊天每轮固定一个合格目标，提供群聊／私聊、号码及可选称呼，失败不换人。旧布局与额度幂等转换并归档，备份携带配置；旧调用、旧快照试跑及自定义模板保留，无 SQLite 表结构迁移。

- 2026-09-10：管理页面重整为 8 项主菜单和顶部二级面板，上下文与提示词成为独立入口。桌面上下文行压缩为 32px，拖动提供占位、插入线和动画，原位、取消及无效落点不改顺序。资料块增加用途、来源与准确管理跳转，可返回原列表；不增加历史逐条溯源，元数据不进入模型。模板、来源、对象、内在状态和调用轮次改为下拉选择，记录分页搜索，正文点击阅读。草稿按面板和对象保留，独立保存不互相覆盖；旧链接、四项 API 视图、业务规则和数据库结构保持兼容。本期按桌面验收。

- 2026-09-10：调试页分为调用记录、模型试跑与提示词模板、上下文注入位置三项菜单，全部模板任务附中文说明，切换和数据刷新保留草稿。上下文支持 system／user 列表拖动、上下移动、角色切换、全局继承和任务独立顺序；原始消息为只读定位行。新闻、搜索、B站、AI日报分别定位，任务材料由统一组装器处理。布局和资料每轮冻结，两种角色的临时资料均不入历史；组合试跑沿用请求快照，旧记录和模板保持原样，备份携带配置，无 SQLite 表结构迁移。
- 2026-09-10：普通消息仅被观察时不再创建聊天调试记录；模型处理或实际发送开始才保存整轮，群观察继续独立工作。自动清理旧纯观察调试轮次及旧备份中的同类记录，避免挤掉最近真实调用；保留调用失败、取消、未捕获原文与实际发送记录。
- 2026-09-10：日记／笔记全文与上下文简报分开保存，生成正文后另调用模型提炼简报；旧全文停止自动召回，可单独生成简报。「记忆与人物」新增相关记忆总条数、日记／笔记条数和简报长度设置，默认 10／2／200，下一次组装生效。正文保留、失败不回退全文，沿用日期、场合和模块隔离。
- 新增可编辑的「本轮群聊回复要求」，默认极短接话，支持恢复默认。默认放在本轮 user 最后、生活资料块外，现可在上下文位置页调整；当前轮冻结文案，工具后继续保留，不入聊天历史。调试来源显示同次文案和位置，私聊与后台任务不附加。
- 自动生活资料移除链接及“阅读依据／来源”字段，保留简短事实边界；角色日常经历统一为“角色经历（18：00）：正文”，仅注入角色时区今天的经历，清理旧前缀并合并同一事件的记忆副本。历史来源及调试原文保留。
- 白名单检查结果改为紧凑摘要，保留接入状态、人格和最近实际注入；会话及历史来源详情可折叠，检查与调试入口并排，手机输入项采用两列布局。
- 阶段编辑改为紧凑行布局，范围、结束值、想法和操作并列；手机采用两行布局，缩减输入框高度、重复说明和留白。
- 当前值支持行内输入与应用，增长和消耗横向排列，详细规则折叠；顶部数值卡片同步压缩。
- 新增独立“内在状态”模块与页面，程序管理寂寞值和精力。按在线时间连续增长、日程真实行动开始扣减，支持人工设值、增长和消耗配置及完整覆盖 0—100 的阶段编辑。
- 活动细化只收到当前阶段的自然语言想法，模型不收到数值机制，也不控制精力增减。阶段改变不自动重新细化，“必须聊天”仅是强烈提示。
- 取消新闻、搜索和主动聊天的每日活动上限及额度预留，保留每个对象发送上限、冷却、免打扰、实际结果统计和防重。一轮多对象聊天只扣一次，执行前跳过不扣、失败不退、低值归零。
- 内在状态关闭时停止增长、扣减和注入；离线不补涨、每天不重置。行动开始与扣减事务保存，人工来源、试跑、被动回复、插话、天气和独立日报不扣。
- 升级保留已有精力并初始化寂寞值，归档清空未开始活动的旧细化及决定，保留大纲、场合和已开始执行证据；旧日程模板归档后启用新默认，旧备份同样转换，不追扣历史或重放旧行动。SQLite 表结构不变。
- 验证结果与真实联调边界见[交付与验收记录](docs/03-delivery-and-validation.md)。

## [0.2.4] - 2026-09-09

- 正式日程只生成活动大纲；临近活动时结合人格、状态、相关记忆、近期实际结果与剩余额度，细化并决定是否安排新闻、搜索和主动聊天。
- 三类数量改为每日上限，默认 2 次／2 次／3 轮，允许少做；预留与实际使用分别持久化，开始执行后失败也计数，保存上限立即约束未执行行动。
- 支持带本次要求的手动细化与重新细化，成功后才替换旧结果；编辑大纲作废旧细化。自动失败最多再试一次，过期不补做，重新生成整天日程不清零已用次数。
- 自动归档并转换旧活动，备份旧日程模板后启用新默认；统一执行流程，保留历史、实际发送及防重记录。SQLite 表结构不变。
- 页面分开大纲参数与每日上限，展示细化决定、历史及跨版本实际用量；保留四项调试与 JSON 阅读、折叠及原文复制下载。
- 在 Python 3.12.14 / AstrBot 4.28.0 源码环境完成 343 项自动化；12 项 4.27.5 专项因缺少官方源码缓存跳过。桌面、手机页面冒烟与静态检查通过，未进行真实 QQ 发送或在线模型联调。

## [0.2.3] - 2026-09-06

- 调试页面固定四项：上下文来源、API 原始请求、API 原始返回、回复阅读版。多次模型调用和 HTTP 重试逐次配对，原文可直接复制下载。
- 在原有 SDK HTTP 客户端旁路记录 DeepSeek/OpenAI 兼容 Chat Completions 与 Responses；保留未知字段、流式原文、错误与部分结果，旧快照不冒充原文。
- 生活动态资料改为中文段落，移除会话 UUID、历史统计和存储字段；来源与精确注入文本同时生成，保持人格和场合边界。
- 设置入口移至导航 02；聊天保留 10 轮、后台每类 10 次完整调用，试跑与公共模板继续分开。
- 使用真实 Provider、SDK 和本地 HTTP 服务对照收发正文，覆盖重试、流式、工具后调用、并发、取消及热开关；真实在线模型与 QQ 联调单独记录。

## [0.2.2] - 2026-09-06

修复 4.27.5 私聊已有历史却被判定“人格未解析”的接入错误。

- 补传真实会话的 `provider_settings`，沿用宿主会话规则、当前对话人格和默认人格优先级；白名单、聊天与后台任务共用解析判断。
- 区分连接不存在、平台不支持、人格未配置／禁用／缺失／不匹配、模块关闭和读取异常，显示实际人格、绑定人格及来源；历史读取结果独立显示。
- 区分“配置允许接入”和实际上下文注入，记录最近处理、最近实际注入和关联调试轮次，支持直接打开对应记录；检查结果跨页面保留。
- 聊天包含当前时间、当前活动与当天日程摘要，保留场合过滤；无日程及模块关闭明确标记。
- 用 4.27.5 官方解析函数重现漏传参数并验证修复，与本地 4.28 解析函数分别回归。替换无条件成功的聊天测试替身。
- 244 项自动化及桌面／手机页面测试通过；真实 QQ、在线模型联调仍待完成。无需重建白名单、历史、记忆或日程。

## [0.2.1] - 2026-09-06

对齐群聊、私聊上下文和整轮调用记录，继续支持 AstrBot `>=4.27.5,<5`。真实 QQ 和在线模型联调待完成。

- 未聊过的白名单对象也能接入，页面只读检查真实会话、人格和历史，区分找到历史、首次对话和读取失败。
- 私聊保留宿主当前对话；角色补充资料与本轮生活、记忆分开，动态资料不写进聊天存档。主动私聊发送成功后写入当前会话，首次联系按需建立会话。
- 群聊独立观察最近 24 条、12,000 字符，即使宿主群历史或插话关闭也可积累。生成时去掉重复宿主群历史，保存前恢复；群内成员仍按真实会话保留人格和记忆边界。
- 每轮关联收到消息、接入、上下文、各次 Provider 调用、工具和实际发送；补齐工具后的模型调用、流式结果、并发隔离、取消和部分发送状态。
- 聊天默认保留 10 轮完整记录，后台每类 10 次。新记录不再同时保存 `response` 与 `reply`，修复消息组件缺少正文；原始返回和解析结果分开展示，旧记录可读。
- 调试写入失败不阻断正常模型与发送。关闭调试和卸载恢复调用包装；清理调试不删除业务数据或原始会话。
- 新增聊天回归及页面测试，补充 4.27.5 官方 Agent、消息保存和会话锁接口核对；自动化与真实联调分别记录。

## [0.2.0] - 2026-09-05

日程、来源、调试与首页重构。继续支持 AstrBot `>=4.27.5,<5`；本轮为可安装测试交付，真实模型、QQ 和外部账号联调待完成。

- 默认 06:00 生成唯一正式日程，精确校验 10 个活动及新闻 2、搜索 2、聊天 3 个标记；支持重叠与未开始活动的配额内调整。
- 日程按新闻、搜索、聊天顺序执行，保留原始生成 JSON、参数和完整请求，展示计划、成功及跳过结果；取消独立随机探索和临时主动联系。
- 增加「聊天对象与白名单」，普通表单自动处理会话 ID，兼容旧配置；按真实目标抽选，按真实会话保持人格及记忆边界。
- 天气固定和风，增加连接测试、90 分钟刷新及 15 分钟失败退避。B 站固定 Bilibili AI Bot，补充只读视频信息核验。
- 内置六个新闻源和黑鸦 Heya 12:00、橘鸦 Juya 23:00 两路独立 AI 日报；搜索沿用宿主配置，新闻和搜索保存选题、事实、感想及阅读依据。
- 增加完整调用记录、每类默认 10 次保留、JSON 导出、隔离试跑及任务模板编辑。明确宿主调用、被动回复钩子和第三方结果的捕获边界。
- 首页优先展示角色状态、当前活动、时间线和下次联系；配置移入独立入口，按钮说明真实调用与影响范围，「维护记忆」更名为「整理旧记忆」。
- 升级保留旧数据与当天日程，补充配额、隐私、模块启停、重启、调试隔离及桌面/手机页面回归。
- 核对 AstrBot 4.27.5 官方发布标签中的 Web API、模型调用、工具注册与发送后钩子；自动化环境仍为本地 4.28.0-beta.1，未混称为 4.27.5 实测。

## [0.1.0] - 2026-09-05

首轮实现人格接入、轻量生活、分类记忆、日程行动、主动社交、外部见闻、日记、Plugin Pages 及恢复能力；放开 4.27.5 安装限制。

当时的临时主动社交、任意天气 URL 和手填搜索工具方案已由 0.2.0 替换，旧版行为不作为当前配置依据。

## [0.0.1] - 2026-09-04

建立插件入口、健康检查、主程规划、工程约定与 MIT 许可证；后续文档整理为一份总计划和八张能力任务卡。
