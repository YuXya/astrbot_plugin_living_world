# Living World 开发协作规则

> 2026-09-11 最新规则：当前仅使用已保存的现行格式：布局 v4、用量 v2、统一记忆 v2；新安装缺失项使用当前默认值，不执行旧配置、旧日程、内在状态或旧记忆迁移。备份格式为 v2，旧备份直接拒绝，不转换或重放旧任务。
## 开始工作

先读[总计划](docs/01-lead-plan.md)、[任务索引](docs/README.md)及相关任务卡，再核对[安装测试说明](docs/02-install-and-test.md)和[实际验收记录](docs/03-delivery-and-validation.md)。当前为 0.2.6；用户最新明确要求优先于历史文档。修改前检查工作区，保留已有改动。

## 产品边界

- 单插件、单角色，沿用一个 AstrBot 人格，接入使用该人格的指定 OneBot QQ 群聊与私聊；核心人格由用户维护。
- 日程范围由 `life.schedule_start/schedule_end` 设置，默认 08:00—24:00，使用角色时区，当天结束；允许中间空档，首条必须从开始时间起、末条必须到结束时间止，所有活动时长为正且不重叠、不越界。生成、重生成和未来活动编辑共用校验，不拉长或补造活动；24:00 仅作结束时间，内部为次日零点，日程显示为 24:00。参数按日冻结，保存默认次日生效，手动重生成成功才替换今天；旧日程和自定义模板不迁移或自动重写。
- 范围外统一投影为睡梦中，地点沿用角色默认；不生成正式睡眠活动、经历或记忆，范围内空档显示当前未安排活动。停止本插件主动聊天和群聊插话，保留普通被动回复与现有免打扰。生成前、模型排队后及发送前再次检查，私聊取得宿主会话锁后也检查；开始前跳过不扣，已开始生成后不退还。关闭日程模块不应用新增限制；新闻、搜索、日报及记忆后台任务不增加限制。
- 正式日程每天自动生成一次活动大纲，默认 06:00、10 个活动；大纲不生成新闻、搜索或主动聊天。默认提前 10 分钟细化，结合活动、状态、相关记忆、当前想法和限制决定行动；允许全部不安排，每个活动每类最多一次，可多类共存，按新闻、搜索、聊天执行。三类行动无每日活动上限，不预留额度，保留实际行动统计和防重。
- 内在状态独立管理寂寞值与精力，范围 0—100，默认每在线小时各加 10；离线不补涨、每天不重置、封顶不积攒。日程聊天首次通过最终检查并开始生成消息时每轮减寂寞值 10，每轮固定一个目标、只扣一次；日程新闻和搜索通过能力检查并开始调用时分别减精力 10。速率及各项消耗可设，执行前跳过不扣，开始后失败不退，不足允许执行并归零。人工来源调用、被动回复、插话、天气和独立日报不扣。
- 数值、阶段范围、名称和增长消耗机制不投影到模型；“当前阶段想法”默认仅活动细化勾选，其他任务可自行勾选，只提供本轮开始时命中的两个自然语言文案，AI 不控制增减，移除 energy_delta。阶段完整覆盖 0—100，可拆分、合并、编辑文案；“必须聊天”只是强烈提示，程序不强制行动。模块关闭停止增长、扣减及注入，保留数据并允许人工调整；普通安装升级默认启用，备份恢复后关闭。
- 未开始活动支持细化、重新细化及仅用于本次的临时要求；合格后事务替换，失败保留旧结果。修改大纲后旧细化归档作废；自动细化失败后，在活动开始前至少间隔 60 秒再试一次，次数持久化。数值或阶段改变不自动重新细化。公共细化不读取其他场合的私人谈话或发送正文。
- 管理员可显式重新生成今天整份日程，用最新已保存参数和模板，不受生成时间限制。校验成功后事务替换，旧版本与真实执行记录归档，失败保留旧日程；未来活动细化后照常执行，内在数值、实际行动记录和发送限制不重置，过期行动不补做。重生成与日程执行互斥，旧备份不能恢复已退役活动的执行资格。
- 不恢复独立随机阅读、随机搜索或临时主动联系。被动回复和群聊插话独立控制；管理员明确发起的来源读取须说明真实调用与保存结果。
- 群聊、私聊、主动聊天分别维护独立回复要求，在 05 聊天与对象 → 回复与插话修改或恢复默认，各自最多 8000 字符、留空不注入。三份文案各自保存，缺失项使用各自的当前默认文案。默认分别用于 chat.group、chat.private、social.message，插话判断默认不勾选；所有任务可自行勾选三份要求。位置遵循全局排序，始终独立于生活资料块；临时内容不入历史，工具后的请求继续使用本轮快照。这是模型提示，不截断回复或修改发送流程。
- 社交按白名单权重每轮固定抽取一个合格群或私聊目标，失败不另抽，沿用频率、免打扰和防重。「聊天白名单：交谈对象与场合」只显示类型、目标名字／群聊名字和接收范围，不显示目标号码或内部连接标识。优先白名单手填称呼，否则通过对应 OneBot 连接只读昵称或群名；成功缓存一小时、失败一分钟，按连接及目标隔离，总超时两秒，失败显示“未设置名字”，群名不回退群成员名。号码继续用于管理、匹配和发送，名字中的合法数字保留；目标匹配与实际会话的人格/记忆判断不能混为一谈。
- 今日日程有完整及简版两个资料行，每任务最多勾选一种，也可全不勾选。群聊、私聊及 social.message 默认简版；有当前活动时取上一条、当前条、下一条，否则取最近两条过去活动及下一条未来活动。先按角色时区今天与当前场合过滤，排除取消或退役活动，最多三条、不跨日补齐；保留完整可见正文、时间、真实状态、地点及睡眠，不另调用模型或截短正文。完整及简版来自同次日程快照。
- 未聊过的对象也可接入；历史检查只读。私聊沿用当前对话，动态资料不入存档；成功主动私聊写入宿主历史。群观察独立于插话和宿主群历史开关，限制 24 条、12,000 字符；去掉重复历史后须在保存前恢复，失败回退宿主。
- 认识同一个人，谈话分场合。具体谈话、私人约定及其日程、日记、笔记衍生内容保留来源场合，不因全局生活计划或分享而泄露。
- 天气固定和风，B 站固定 `astrbot_plugin_bilibili_ai_bot`。B 站仅搜索、指定视频观看、公开视频记忆 API v3及必要只读视频信息；不调用自主浏览、互动或私信流程。
- 新闻有六个默认 RSS 来源，可逐项修改、启停和恢复。搜索沿用宿主配置。两路 AI 日报独立定时，每路每天一次，不消耗内在状态、不额外聊天、不重启补读。
- 计划、角色虚构经历、真实来源证据和实际发送分别记录。失败不得写成成功，发送不等于得到回复，搜索或旧记忆不等于新观看。
- 各业务能力独立启停，关闭保留当前数据；不重置已保存数值、日程、模板、记忆或发送防重记录。不再执行历史升级转换。

## 实现与分工

- 记忆提炼使用持久化批次与逐条事务回执，首次失败后至少间隔 60 秒自动重试一次，之后停止等待手动处理。修复只接收未成功候选的编号，空数组不解除待修复项；完成和无新增均保留来源防重标记，完成后清理临时正文。反馈失败不回滚合格记忆，不重放已处理反馈。
- `memory.reflect` 默认仅勾选本次任务材料、记忆与画像，保留 AstrBot 原始人格；取消补充资料、世界、当前状态和时间，仍允许手动加回。新材料只投影一次，背景身份去重且最多 10 条／12,000 字符；旧记忆仅对照。依据允许概括，`source_ids` 必须关联本批明确来源；原始来源继续决定归属、时间及跨场合稳定规则。默认模板包含六组合法 JSON 输入输出案例。
- 提炼进度每页 20 条，正文按需读取；查询和局部刷新不启动模型、遗忘或全站状态读取。手动重试显式开启新一轮额度。调用记录分别保留模型结果与记忆处理结果，关联批次，不更改 HTTP 原文或推断旧记录。当前配置及自定义模板不自动重写，安装后用户可恢复该任务默认勾选／模板。

- 记忆管理默认采用多人物卡片首页与独立档案：当前自身、QQ 人物、历史自身明确区分，卡片 12／页；五属性默认展开各 10 条可续载，时间视图和全库列表 20／页。单条记忆卡片按参考画像页的紧凑层次展示星标、强度、实际时间、完整结论、事实依据与全部标签：内边距 10px／12px、结论 14px、依据 13px、卡片间距 10px，不截短正文或标签。点击后右侧滑出完整详情，归属、场合、有用分、保留状态、版本及编辑操作在详情中呈现；来源和版本按需读取，关闭恢复列表位置。保留 Living World 配色，不新增 AI 概述，来源缺失不补抓聊天或调用模型。
- 管理查询使用管理员 action 接口，摘要复用对象存储轻量投影缓存，全局状态只保留记忆计数；昵称在首屏之后异步补全，沿用手填优先与连接级缓存、两秒时限。分页、搜索、名字和来源读取不强化、不遗忘、不启动后台任务。记忆编辑只更新受影响记录与统计，校验版本，保留原归属、场合、分页和草稿；历史自身不新增记忆。页面重写不修改总结、召回、排序及已有记忆数据，不引入业务表迁移。
- 本轮用户明确限定仅运行静态检查及安装包语法校验，不运行 Python 回归、桌面冒烟或在线联调。只修改开发仓库与本地安装包，Git 提交、发布及实际安装更新由用户处理。

- 模型试跑已删除：不提供建立、预览、执行或复制到编辑器的入口。调用记录仅供查看、复制、下载和清理。上下文目录中的任务日期、参数、原因、问题和材料供正式业务使用，归于提示词模板入口；保留其标识、顺序与勾选，不恢复试跑模块。
- 主程统筹共用能力、依赖、集成与验收；一个主会话、最多三个子智能体并行。按能力分配代码范围，共用文件提前协调。
- 使用 Python 3.12，安装版本范围 `>=4.27.5,<5`。当前本地测试环境为 Python 3.12.14 / AstrBot 4.28.0；4.27.5 官方源码核对与该版本实机验收分开记录。
- 入口保持简洁，优先使用 AstrBot 的人格、模型、会话、消息和工具能力；Plugin Pages 是唯一管理配置入口，不增加 `_conf_schema.json`。
- 原始聊天存档沿用宿主；业务库另存有上限的群观察窗口、提炼记忆与生活记录。管理员调试记录按聊天整轮默认保留 10 轮、后台每类 10 次，清理不得删除群窗口、原始历史、正式档案和防重记录。
- Provider 记录适配必须按轮次隔离并可卸载；工具后的模型调用分别记录，跳过第三方工具内部调用。调试失败不能影响正常回复，发送成功和存档成功分别记录。
- 解析会话人格必须传入该 UMO 的 `provider_settings`，沿用宿主优先级；4.27.5 默认人格依赖该参数，不能只用 4.28 或固定返回人格的替身验收。页面配置通过与实际注入分开，日程摘要使用场合过滤后的视图。
- 调试固定四项：同次来源与注入文本、API 请求原文、API 返回原文、回复阅读版。DeepSeek/OpenAI 兼容 Chat Completions 与 Responses 在现有 SDK HTTP 客户端旁路记录；未知字段保留，SDK HTTP 重试逐次配对。旧快照、未捕获请求不得冒充原文；SSE 保留事件流，合并仅供阅读。只隐藏已知认证凭据，不按同名普通字段误删正文。
- 动态生活资料用中文段落，去掉 UUID、无关历史统计与数据库字段；内在状态只向勾选的任务提供本轮开始时命中的文案，不提供数值机制。来源清单与组装同次生成，不能事后重新检索来推断。原文先保存，阅读解析失败不能丢失原文。关闭记录终结在途流，重新开启不能把缺段的旧流记成完整。不要把私人动态上下文自动保存为公共模板。
- 动态上下文、记忆条目、日程材料及当前默认写作模板不再追加角色日常属于虚构、不是网络事实或计划未发生的重复说明；活动状态仍显示计划中、进行中、已结束、失败等实际状态。普通聊天历史的轮数与模型窗口限制沿用 AstrBot，不把记忆提炼批次当作聊天历史上限。
- 统一记忆包含当前人格名称对应的自身画像和按 QQ 身份识别的人物画像。人格改名后自身档案从空开始，旧档保留，改回原名继续使用；人物档案不受影响。五种属性为用户别名、事实属性、技能树、关系图谱、活跃项目，自由检索标签；保存结论、事实依据、稳定／推断标记、来源、原始时间、场合和版本。核心人格由用户维护，具体谈话和约定不跨场合泄露。
- 聊天默认累计 5 轮或闲置 10 分钟总结，参数可设，只用角色参与的交流与最终回复。经历、上网摘要、日记和笔记保存后进入统一后台提炼；保留上网事实摘要与感想，取消旧七类记忆及独立日记／笔记简报。AI 可提出新增修正合并，程序校验归属和场合后事务应用，失败保留原件，旧版本停止召回。队列和来源处理标记持久化，成功清理临时聊天材料；日记按目标日期、笔记按主题从新记忆取材，无材料跳过。
- 上下文名称统一采用真实二级菜单前缀。记忆与画像、近期记忆两个独立资料行取代旧七类、综合见闻、近期经历及独立回顾／日记原文资料行，原记录页面保留。用量版本 2：自身稳定画像 5、每人画像 5、相关记忆 10、近期记忆 5、群聊最多人物 3、天气 1；0 停止对应注入。近期记忆按真实时间跨日取数并优先去重，未勾选项不占额度、不影响其他项。布局版本 4 保留唯一全局 order、baseline_order 和 tasks 勾选集合；新行承接对应旧位置，其余全局位置与默认基准保留，旧配置归档。初始化、重载和备份导入仅使用当前格式，不含升级转换；历史调用原文可只读查看，无 SQLite 表结构修改。
- 稳定画像按身份读取；话题召回通过普通模型理解检索词后使用中文词片 BM25 搜索标签和结论，默认最多等待 15 秒，失败退回关键词检索，不依赖向量。所有业务共用流程，内部记忆步骤直接检索避免递归。只选近期记忆不增加模型调用。主动记忆与主动召回共用权限；总结反馈只处理本轮确实提供的记忆，每轮每条一次，反馈缺失或失败不奖惩，取消原自动强化。
- 遗忘默认关闭，只累计插件在线、记忆模块与遗忘开关开启时间，离线或关闭不补扣。普通记忆初始强度 10，低档每 3 在线日减 1；有用反馈默认强度加 1、分数加 2.5，达到 3 分只因无用反馈淡化，达到 10 分长期保留；参数可设。主动记忆和手动重要项保护。遗忘彻底删除正文、版本和索引关联，原来源与防止自动重新入库的必要标记保留。
- 不执行旧资料迁移或重提炼；备份携带当前配置、档案、来源处理标记、正常批次及进度，通过现有对象存储保存，不新增 SQLite 业务表。
- 管理页面分 8 个一级菜单及顶部二级面板（见 docs/05-admin-navigation.md），02 上下文与提示词含上下文排序、上下文用量、提示词模板、调用记录；任务名统一为原标识加中文说明。桌面上下文行高 32px，拖动具有占位及换位动画，取消或无效落点不改顺序。静态来源索引跳转管理入口，不追溯具体历史记录、不进入模型。全页面草稿按面板和对象隔离保留，只保存本面板字段，不写本地存储。所有任务显示同一完整资料列表，除只读原始人格及消息定位行外均可勾选、拖动、上下移动和切换 system／user；“调整范围”只切换任务勾选项，任意任务下排序均修改唯一全局顺序。提供保存上下文设置、恢复本任务默认勾选、恢复默认排序；恢复先改草稿，切换任务、来源跳转及保存失败保留草稿。通用资料按本轮场合读取，任务专用材料只用本次明确提供项、缺失跳过；保存勾选不触发新闻、搜索或其他任务；相关记忆在实际业务调用中按需理解检索词，继续遵守模块、日期、场合及用量限制。未勾选记忆不强化，不参与其他已选资料去重。正式调用统一组装，每轮冻结顺序、勾选、资料和回复文案；两种角色的临时资料均不入历史，工具后沿用快照，①只记实际注入且与②一致。旧记录不重组、旧模板不改写，无表结构迁移。
- 代码、提示词、页面和测试独立编写；参考项目只用于理解能力，不复制表达性内容。实际引入资源时核对来源与许可。
- 不要求固定工位、全消息总线或逐项 ADR；任务卡只写内容、依赖和验收效果，实现细节随开发处理。

## 验证与交接

- 单独日程设置采用轻量保存回执，当日参数冻结和设置保存同一事务；不运行维护、重排或取消在途生成。页面区分当前采用范围与已保存的下次生成范围，保留失败草稿。日程范围检查只读取缓存的轻量参数，不反复加载完整生成档案。
- 纯模板保存／恢复、上下文设置及只改回复文案不依赖全量状态刷新；活动 JSON／单条编辑只更新相关日程档案，角色状态及记忆变更查看各自更新所需内容。保存失败与页面往返保留草稿。人物画像的 QQ 昵称只读显示，复用对应连接缓存并限制整页等待；不得借昵称修复改写历史记忆归属。

- 优先验证内在状态在线增长、边界与阶段编辑、实际开始事务扣减、单目标抽选与每轮单次扣减、离线与关闭冻结、模型输入无数值、多类行动共存、细化失败与重试、场合隔离、当前格式备份和重启防重复。
- 自动化使用替身；真实消息只向已明确配置的测试白名单发送。没有凭据或连接时明确记录未实测，不虚构验收结果。
- 运行必要测试和静态检查，页面本期按用户最新要求优先验收桌面，手机适配不作为交付门槛。交接说明实现、验证、剩余风险和真实联调待办。
- 英文代码注释、文档字符串及日志；中文用户文档。若用户要求提交，使用 Conventional Commits。
- 不提交密钥、Cookie、完整私聊、数据库、调试导出或缓存；安装包仅含源码、页面、依赖声明及文档。
- 公开发布、部署及插件市场上架另行安排。
