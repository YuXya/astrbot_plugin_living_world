# Living World 开发协作规则

## 先读什么

- [主程总计划](docs/01-lead-plan.md)：产品目标、开发任务、依赖顺序与整体验收。
- [任务索引](docs/README.md)及本次涉及的能力任务卡。
- [安装测试说明](docs/02-install-and-test.md)及[交付验收记录](docs/03-delivery-and-validation.md)：0.1.0 已实现能力、自动化验证和真实联调待办。
- 开始修改前确认当前实现与工作区改动；任务卡描述能力范围与验收目标，实际验证状态以交付记录为准。

## 产品边界

- 单插件、单角色，独立提供生活与记忆；首发验证 OneBot QQ 群聊和私聊。
- 沿用 AstrBot 人格，插件补充角色资料、世界设定与动态生活上下文。核心人设固定，不要求空白人格。
- 日程可以安排阅读、搜索、主动找群或个人聊天；模型也可以临时调用这些能力。实际执行成功后才记录对应真实行动，不能把计划当成已发生。
- 主动对象从白名单按权重概率随机抽选，数量可配置。日程社交、临时主动调用和生活分享使用同一套抽选与发送控制。
- 允许聊天形成长期记忆并影响日程。认识同一个人，谈话分场合；私人谈话不能自动成为其他场合的分享内容，也不能通过日记或自身体会绕过场合限制。
- 新闻、搜索和天气来源可配置和替换。当前 B 站适配依赖 `astrbot_plugin_bilibili_ai_bot` 的 `search_bilibili`、`watch_video` 与公开记忆 API v3，只读取公开视频记忆；不得调用完整自主浏览、互动或私信流程。
- 所有业务能力可以在运行时独立开关。关闭保留数据并停止对应行为；单模块失败不拖垮其他能力。

## 分工与实现

- 主程负责共用能力、模块边界、跨模块协调、联调和整体验收；按任务分配负责人，不固定人数或并发工位。
- 任务卡只规定做什么、依赖什么、完成效果。具体实现由任务负责人提出，涉及共用行为时与主程及相关负责人协调。
- 模块通过明确的能力入口协作，不直接篡改其他模块的内部状态。通信方式按实际需要选择，不强制全部经过消息总线。
- 不要求每次变更先写 ADR，不预先冻结表结构、事件字段或算法。必要的技术说明随实际实现维护。
- 保持入口简洁，优先使用 AstrBot 提供的人格、模型、消息与会话能力。原始聊天历史沿用 AstrBot；插件维护提炼后的记忆、生活记录及必要运行状态。
- 以 Python 3.12、AstrBot 4.28.0-beta.1 为首轮验证基线。Plugin Pages 是唯一配置入口，设置使用插件数据库；不要增设会与页面冲突的 `_conf_schema.json`。
- 使用可替换的外部接入；未配置或停用某项能力时，清楚显示状态，不伪造成功结果。
- 本项目独立编写实现、提示词、页面与测试；参考项目用于理解能力和行为，不复制其表达性内容。实际引入第三方依赖或资源时核对来源与许可。

## 验证与交接

- 对实现的行为运行相应测试，优先覆盖总计划中的生活、记忆、社交、模块启停和恢复场景。
- 外部调用和消息发送优先用替身验证；真实消息只向明确配置的测试白名单发送。
- 交接说明已完成内容、验证结果、依赖与剩余问题。共用文件变更提前协调，保留其他人的工作。
- 代码注释、docstring 和日志使用英文；用户文档使用简明中文。提交使用 Conventional Commits。
- 不提交密钥、Cookie、完整私聊、运行数据库或缓存。明确区分已实现、自动化验证与真实联调，不能把未做的实测写成已验收。
- 发布和插件市场上架另行安排，不因文档或阶段任务完成而自动发布。
