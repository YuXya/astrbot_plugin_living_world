# Living World 开发协作规则

## 开始工作

先读[总计划](docs/01-lead-plan.md)、[任务索引](docs/README.md)及相关任务卡，再核对[安装测试说明](docs/02-install-and-test.md)和[实际验收记录](docs/03-delivery-and-validation.md)。当前为 0.2.0；用户最新明确要求优先于历史文档。修改前检查工作区，保留已有改动。

## 产品边界

- 单插件、单角色，沿用一个 AstrBot 人格，接入使用该人格的指定 OneBot QQ 群聊与私聊；核心人格由用户维护。
- 正式日程每天一次，默认 06:00、10 个活动、新闻 2、搜索 2、聊天 3 个标记。允许重叠，按新闻、搜索、聊天执行；未开始活动才能在原配额内调整，保留执行记录。
- 不恢复独立随机阅读、随机搜索或临时主动联系。被动回复和群聊插话独立控制；管理员明确发起的来源读取须说明真实调用与保存结果。
- 社交按白名单权重随机抽取不同目标，共用数量、频率、免打扰及防重规则。目标匹配与实际会话的人格/记忆判断不能混为一谈。
- 认识同一个人，谈话分场合。具体谈话、私人约定及其日程、日记、笔记衍生内容保留来源场合，不因全局生活计划或分享而泄露。
- 天气固定和风，B 站固定 `astrbot_plugin_bilibili_ai_bot`。B 站仅搜索、指定视频观看、公开视频记忆 API v3及必要只读视频信息；不调用自主浏览、互动或私信流程。
- 新闻有六个默认 RSS 来源，可逐项修改、启停和恢复。搜索沿用宿主配置。两路 AI 日报独立定时，每路每天一次，不占日程配额、不额外聊天、不重启补读。
- 计划、角色虚构经历、真实来源证据和实际发送分别记录。失败不得写成成功，发送不等于得到回复，搜索或旧记忆不等于新观看。
- 各业务能力独立启停，关闭保留数据。升级保留当天已有日程、记忆、白名单和防重记录。

## 实现与分工

- 主程统筹共用能力、依赖、集成与验收；一个主会话、最多三个子智能体并行。按能力分配代码范围，共用文件提前协调。
- 使用 Python 3.12，安装版本范围 `>=4.27.5,<5`。本地测试环境为 Python 3.12.14 / AstrBot 4.28.0-beta.1；4.27.5 官方源码核对与该版本实机验收分开记录。
- 入口保持简洁，优先使用 AstrBot 的人格、模型、会话、消息和工具能力；Plugin Pages 是唯一管理配置入口，不增加 `_conf_schema.json`。
- 原始聊天历史沿用宿主；业务库保存提炼记忆与生活记录。经用户授权的管理员调试记录独立保留完整调用参数，默认每类 10 次，清理不得删除正式档案和防重记录。
- 调试界面明确 AstrBot 参数、宿主钩子快照和第三方返回的边界，不能冒充提供商 HTTP 请求或第三方内部调用。隐藏认证字段；不要把私人动态上下文自动保存为公共模板。
- 试跑只调用模型和保存调试结果，不执行工具、不发送 QQ、不改正式业务数据。测试上下文与公共模板分开维护。
- 代码、提示词、页面和测试独立编写；参考项目只用于理解能力，不复制表达性内容。实际引入资源时核对来源与许可。
- 不要求固定工位、全消息总线或逐项 ADR；任务卡只写内容、依赖和验收效果，实现细节随开发处理。

## 验证与交接

- 优先验证配额准确、活动重叠、局部修改、场合隔离、调试无业务副作用、失败隔离和重启防重复。
- 自动化使用替身；真实消息只向已明确配置的测试白名单发送。没有凭据或连接时明确记录未实测，不虚构验收结果。
- 运行必要测试和静态检查，页面检查桌面及手机布局。交接说明实现、验证、剩余风险和真实联调待办。
- 英文代码注释、文档字符串及日志；中文用户文档。若用户要求提交，使用 Conventional Commits。
- 不提交密钥、Cookie、完整私聊、数据库、调试导出或缓存；安装包仅含源码、页面、依赖声明及文档。
- 公开发布、部署及插件市场上架另行安排。
