# 0.2.5 交付与验收记录

最近验证：2026-09-10。交付为**可安装测试版本**，源码实现、自动化验证与真实环境验收分别记录。[总计划](01-lead-plan.md)的真实场景联调仍需后续完成。

## 2026-09-10 统一上下文名称、独立分类用量与单目标聊天

- 名称统一采用实际二级菜单前缀，公共目录贯通全部 **39 个资料块**的排序、来源、适用任务以及新调用①和实际模型标题；七类记忆使用结构化来源和类型识别，旧全文不冒充简报。02 顶部新增上下文用量，06 的存档面板为近期见闻，旧用量链接兼容新位置；仍为 8 个一级菜单、28 个二级面板。
- 七类记忆、近期经历、综合见闻和天气独立限量，初始合计 **28 条只读**；普通类别 0—50、天气 0—1，日记和笔记各自初始 200 字符、范围 50—1000。没有隐藏总限制；验证七类各设 50 时确实返回 350 条，0 停止该类但不删存档，仅强化实际选中项。四类见闻合并按时间取最新，天气独立，明确关联的经历／记忆去重，私人场合与来源模块隔离。
- 每轮主动聊天固定抽取一个合格白名单目标，保留权重、人格、发送限制与防重，失败不另抽；向 AI 明确群聊／私聊、目标号码与可选称呼，群聊面向整个群。试跑使用显式会话，不抽选、不发送、不写业务数据。
- 布局版本 2 在旧位置展开记忆七类，合并见闻继承新闻位置，转换全局和任务独立顺序；旧额度按最大余数法及简报均分规则初始化。旧设置事务归档、升级失败一并回滚；恢复与重启幂等、旧 0 值保留。旧调用名称、API 正文、自定义模板及旧布局快照试跑规则保留，无 SQLite 表结构迁移。
- **554 项 Python 自动化通过，12 项兼容检查跳过**；跳过仍因缺少校验过的 AstrBot 4.27.5 源码缓存。运行环境为 Python 3.12.14 / AstrBot 4.28.0。Ruff、JavaScript 语法、导航与纯排序及差异检查通过。
- 使用真实 AstrBot Provider／OpenAI SDK 与本地 HTTP 服务，对照 Chat Completions 和 Responses 的群聊／私聊标题、条数、角色、顺序及目标说明；验证分类标题只出现一次，四来源合并顺序、隐私过滤、临时上下文不入历史，以及工具中途保存布局和用量后两次请求继续使用原快照。并发轮次分别使用各自配置，第三方工具内部调用排除。
- 完整 Edge 桌面冒烟通过：全部一级／二级入口、39 块来源按钮逐目标定位、十类用量与实时合计、恢复初始值先形成草稿、来源返回、保存失败与刷新保留输入、跨面板保存隔离、对白称呼、旧链接、拖动原位／取消／无效／首尾／跨角色及键盘、减少动画、四项调用阅读与原文回归。1600px 桌面截图检查通过，上下文行 32px、间距 4px，用量单行不超过 44px，无横向溢出；手机不作为本期交付门槛。
- 文档与 0.2.5 测试安装包已更新。没有安装到正在运行的插件，没有调用在线模型或发送真实 QQ；真实宿主 Pages、模型、QQ 和外部账号联调另行安排。以下章节保留此前版本的验收事实，本节描述当前实现。

## 2026-09-10 管理页面二级菜单与紧凑布局

- 管理页面改为 **8 个一级菜单、28 个二级面板**。上下文、模板、试跑、调用记录集中于 02；角色状态、日程、聊天、来源、记忆、系统各自拆开。二级按钮固定在内容区顶部，一次显示一个面板；原页面链接、会话与日程关联调用链接兼容新路由，已清理调用有明确说明。
- 桌面上下文行实测 **32px，间距 4px**，定位行同高。名称省略，来源面板显示完整名称；角色下拉框、上下按钮与拖动手柄同一行。真实鼠标拖动使用指针捕获，显示插入线、占位和其他行换位动画，窗口边缘支持滚动。原位、Esc 取消、无效落点不改变列表，也不产生未保存标记；支持跨角色、定位行前后及明确组末尾落点，减少动画偏好生效。
- 为全部 **36 个稳定资料块**增加用途、来源说明、导航目标和相关任务模板。逐项点击验证管理面板、来源类型、字段高亮、模板选择及返回原布局和滚动位置；此索引不冒充历史调用逐条来源，元数据不进入模型请求。
- 模板按任务与版本选择；来源按类型及新闻源／日报作者选择；白名单、内在状态、调用轮次分别下拉切换。记忆、经历、见闻、日记和发送记录默认每页 10 条，支持搜索筛选和点击正文阅读；日程仍显示当天完整紧凑列表。
- 所有设置按面板和对象维护内存草稿，导航、下拉、来源跳转与刷新保留；保存只提交负责字段，失败保留，成功不清其他草稿。验证回复／插话与发送限制分开保存、天气不覆盖新闻源、白名单逐对象编辑和新增后保持选择、模板和试跑隔离、备份选择跨导航保留、弹窗取消及重载提示，不写 localStorage。HTTP 页面新增对象不依赖仅安全上下文可用的 UUID 接口。
- **527 项 Python 自动化通过，12 项跳过**；新增目录覆盖、元数据隔离及不可变目录测试，原模型请求、场合隔离、工具后快照及真实本地 HTTP 对照回归通过。跳过项仍因缺少经过校验的 AstrBot 4.27.5 官方源码缓存。
- **完整 Edge 页面冒烟、导航／纯排序测试、Ruff、JavaScript 语法和差异检查通过。** 保留并迁移原 14 组 JSON／SSE 显示、原文下载、逐请求折叠、后端真实调试聚合契约与内在状态回归。新增 8 菜单各面板、全部来源入口、真实鼠标取消、排序、草稿隔离、记录分页、日程细化失败保留、编辑作废、简报生成和旧链接验证；最终入口链接修正另有定向补测。页面桥接和业务调用使用替身，检查桌面截图。按用户最新要求，手机适配不作为本次验收门槛。
- 本次未修改运行中的插件、真实数据库，未调用在线模型或发送真实 QQ。生成 0.2.5 源码测试安装包；业务触发、组装及历史规则不变，无 SQLite 表结构迁移。实际宿主 Pages 安装和在线联调仍待进行。操作见[管理页面指南](05-admin-navigation.md)。

## 2026-09-10 调试页菜单与上下文注入排序

- 调试页顶部新增三个单选菜单，默认调用记录；模型试跑、任务模板及旧模板归入第二项，第三项配置上下文位置。全部 16 个模板任务及普通群聊／私聊布局任务显示原标识和中文说明，菜单支持键盘切换。从记录建立试跑、查看试跑结果切到对应菜单；任务切换、数据刷新和页面导航保留试跑、模板、布局及保留数量草稿。
- 新增统一任务名称、资料块目录、适用范围、布局校验和组装器。system／user 列表围绕只读定位行支持拖动、上下移动和切换角色，新闻、搜索、B站、AI日报分别定位；任务证据、日记原文等结构内部保持完整。全局默认、任务完整覆盖、恢复继承和初始位置可保存、重载及备份；非法角色、重复或无效标识拒绝保存。
- 普通聊天、主动聊天、插话判断及全部后台模型任务共用组装器；资料范围、模块开关、用量和日期过滤保持原规则。每轮固定布局、资料和群聊回复要求，工具后的请求继续沿用。system 资料在 Agent 开始时作为不存档的消息片段加入，user 临时片段同样不存档；原消息、媒体与其他插件内容保留。未开始生成时卸载、运行中卸载及宿主内容变化的回退均纳入验证。
- **525 项 Python 自动化通过，12 项跳过**；新增 45 项覆盖目录、非法配置、继承与独立设置、重载及备份、全部模板组装、组合试跑快照与无业务副作用、模块关闭、空材料、并发、私人场合、宿主内容变化及卸载。12 项跳过仍因缺少校验过的 AstrBot 4.27.5 官方源码缓存，不能记为该版本实测通过。
- 真实 AstrBot Provider／OpenAI SDK 对接本地 HTTP 测试服务：Chat Completions 与 Responses 的群聊和私聊中，新闻移至 system、经历排在记忆前、群聊回复要求移至 system，实际 HTTP 位置、顺序与①一致，各块无重复且私人资料不跨场合。工具中保存新布局后，两次真实 HTTP 请求仍使用旧快照；第三方工具内部调用继续排除，原有重试和 SSE 原文回归通过。
- **Ruff、JavaScript 语法与差异检查、完整 Edge 页面冒烟通过**。实际检查 1600px 桌面和 390px 手机截图；验证拖到定位行之前、角色切换、上下按钮与键盘操作、任务适用块、复制独立列表、恢复继承、未保存重置、模板中文名和草稿保留，无横向溢出。页面后端桥接使用替身。
- 本轮未调用在线模型、未发送真实 QQ、未改运行中的安装或业务库。沿用 0.2.5 测试包，布局随备份保存，旧设置自动补默认，旧调用记录不重组、旧模板不改写，无 SQLite 表结构迁移。真实宿主 Pages、在线模型及 QQ 安装后联调仍待完成。

## 2026-09-10 排除普通消息的纯观察调试记录

- 聊天收到消息时仅保留本轮内存资料、群观察与接入状态；开始模型处理或实际发送时才创建调试轮次，①保留原始收到消息与同次观察诊断。纯观察不占最近聊天名额、不显示无效调试跳转；关闭调试或清理后的旧轮次不会被后续回调重新创建。
- 加载、保留清理和恢复旧备份时，按明确旧版标记与整轮步骤识别纯观察记录，事务删除且不计入保留名额。模型准备失败、调用失败或取消、HTTP 捕获未适配、工具和发送记录继续保留；未知记录保守保留，不以②③④为空直接删除。
- 对实际运行库进行只读核查，并在内存副本执行清理：移除 **10 轮纯观察、20 条调试明细**，其余 **87 条明细逐条不变**。未修改真实数据库或已安装插件；更新安装包并重载后才停止旧入口记录并清理存量。
- **480 项 Python 自动化通过，12 项跳过**。新增 20 项覆盖连续普通消息不挤掉实际调用、群观察和接入状态、模型阶段仅创建一次、原始消息快照、无模型的真实发送与失败、调试关闭再开启、旧记录清理顺序、幂等和备份恢复，以及不完整 API、失败和未知记录保护。12 项跳过因缺少校验过的 AstrBot 4.27.5 官方源码缓存。
- Ruff、差异检查和完整页面冒烟通过，页面检查覆盖 1600px 桌面与 390px 手机及四项调试视图。本地 Python 3.12.14 / AstrBot 4.28.0，模型和传输使用替身，HTTP 使用本地服务；未新增在线模型或真实 QQ 发送测试。继续交付 0.2.5 源码测试包，无 SQLite 表结构迁移。

## 2026-09-10 日记简报与上下文用量

- 定位到日记生成后整篇正文被再次写为“感受”记忆，普通聊天召回固定 10 条、日程固定 12 条且无日记专属限制；后台记忆提炼还直接取最多 16 条旧记忆原文。本次统一自动召回入口与数量限制，日记／笔记只写简报派生记忆。
- 完整正文先存档，再调用一次模型提炼简报；简报及派生记忆事务采用。简报失败、取消、关闭模块或期间删除不覆盖已有结果或复活旧记录，完整正文保留。旧全文派生记忆在召回前跳过且不强化，可在日记页显式生成简报；无升级批量模型调用、无表结构迁移，不修改历史调试。
- 「07 记忆与人物 → 上下文用量」可设置总条数、其中日记／笔记条数与简报最长字符，默认 10／2／200。降低数量与长度在下一次组装时约束旧简报，0 停止相应召回，后台已知记忆也遵守；不限制管理存档、其他资料区或宿主历史。日记页展示简报，折叠查看完整正文，提供生成及重新生成简报入口。
- **460 项 Python 自动化通过，12 项既有兼容检查跳过**。新增 20 项覆盖超长原文保留、简报及链接清理、旧全文提前过滤、总量与日记子上限、长度热修改、重启配置、日期与私密场合、正式与试跑资料、试跑无业务写入、失败保留、并发去重、删除／停用／取消、真实 SQLite 事务回滚和参数校验。12 项跳过仍来自缺少校验过的 AstrBot 4.27.5 官方源码缓存。
- Ruff、JavaScript 语法、差异检查和完整页面冒烟通过；新增页面验证保存用量、简报生成参数、正文保留和折叠、安全文本显示，检查 1600px 桌面与 390px 手机截图，无横向溢出。
- Python 3.12.14 / AstrBot 4.28.0；模型、页面桥接和传输使用替身，未进行真实 QQ 发送或在线模型联调，也未修改已安装插件。原文与简报事实质量仍取决于实际模型；程序保证长度及数量边界。安装包继续为 0.2.5，包含源码与页面及说明，不含测试运行数据和截图。

## 2026-09-09 可编辑的本轮群聊短回复要求

- 在「02 角色、模型与模块 → 群聊回复」提供「本轮群聊回复要求」，默认要求通常一句短句、只回应一个重点、不分段、不扩写生活资料，工具说明和结果也简短表达。文案可修改、恢复默认或留空关闭，最多 8000 字符；保存后下一轮生效，当前轮使用开始时的快照。
- 注入位置固定为本轮 `user` 消息最后，位于 `</living_world_context>` 之后、生活资料块之外；Agent 开始时将本插件临时块移到后加入的内容和图片之后。①独立列出同次实际文案、设置出处和位置，②保留真实 API 请求。要求不进入 System、不写入宿主历史，仅用于已接入的普通群聊回复；没有增加回复截断、额外模型审查或修改工具阶段发送行为。
- **440 项 Python 自动化通过，12 项跳过**。覆盖配置校验与 SQLite 持久化、旧配置默认值、留空关闭、私聊与后台排除、单轮快照、重复钩子、后加入内容与图片的排序、历史恢复失败回退、调试关闭、临时资料不入历史，以及真实宿主 Chat Completions／Responses Provider 发往本地 HTTP 服务的正文和工具后续调用位置。跳过项仍因缺少校验过的 AstrBot 4.27.5 官方源码缓存。
- Ruff、JavaScript 语法检查、差异检查和完整页面冒烟通过；页面覆盖编辑、保存、导航后读取、恢复默认及安全文本显示。已检查 1600px 桌面和 390px 手机截图，无横向溢出。
- 本地环境为 Python 3.12.14 / AstrBot 4.28.0，模型与消息传输使用替身，本次未调用在线模型、发送真实 QQ 或修改已安装插件。短回复要求属于生成提示，不保证模型绝对遵守；真实回复长度及宿主多阶段发送仍需安装后观察。沿用 0.2.5 测试安装包，无数据库表结构迁移。

## 2026-09-09 上下文链接与重复经历清理

- 普通聊天、主动聊天、日程生成、调整、细化与自动试跑资料清理链接及阅读依据字段，保留必要事实边界；来源总结所需证据、用户消息、宿主历史、管理存档与历史调试原文保持完整。
- 修复活动、派生记忆及上下文重复加前缀的问题，统一输出“角色经历（18：00）：正文”。只注入角色时区今天的角色日常，关联事件优先恢复旧记录发生时间；知识、人物认知和约定不受今天过滤影响。同一事件在记忆与经历之间去重，旧记录只清理展示副本，新事件和派生记忆事务保存共同时间及关联。
- **424 项 Python 自动化通过，12 项跳过**。本次新增 23 项，覆盖旧前缀、空条目、正文和链接清理、同事件去重、不同发生时间、午夜及时区夏令时、缺失日期、旧关联时间、召回限额、私人场合、SQLite 事务回滚与重启、正式及试跑资料一致，以及真实 Provider 调用入口中用户历史、来源原件和旧调试不变。12 项跳过仍因缺少校验过的 AstrBot 4.27.5 官方源码缓存。
- Ruff、JavaScript 语法检查、差异检查和完整页面冒烟通过；已检查 1600px 桌面与 390px 手机调试截图，无横向溢出。测试使用替身和本地 HTTP 服务，未进行真实 QQ 发送或在线模型联调。沿用 0.2.5 测试包，无 SQLite 表结构迁移或历史批量重写。

## 2026-09-09 白名单检查结果信息密度优化

- 移除逐条提示框及叠加留白，以紧凑摘要展示历史、配置接入、人格和最近实际注入；同次重复处理信息归入详情，拒绝原因及历史读取错误保持直接可见。
- 会话标识、真实场合、历史来源、对话编号与聊天轮次默认折叠，支持键盘展开，重新检查保留展开状态。检查与调试入口并排，手机输入项采用两列布局，长文本自动换行。
- JavaScript 语法检查、差异检查和完整页面冒烟通过；已检查 1600px 桌面、390px 手机的收起与展开截图，无横向溢出。桌面测试对象收起约 280px、展开约 420px。本次仅修改前端展示和说明，未重新执行 Python 全量测试或真实 QQ 发送。

## 2026-09-09 内在状态页面信息密度优化

- 阶段卡片改为紧凑行：桌面将范围、短结束值输入框、想法和操作并列，手机分成两行。合并重复说明，想法默认单行并随内容增高，减少卡片间距与留白，保留原有阶段增删及分界规则。
- 当前值改为行内输入与应用，增长和各项消耗横向排列、窄屏自动换行；详细计算及生效规则默认折叠，顶部数值卡片同步减少间距和高度。保留独立保存、暂停说明及草稿行为。
- JavaScript 语法检查、差异检查和完整页面冒烟通过；1600px 桌面与 390px 手机截图已检查，无横向溢出。桌面默认阶段高度约 80px。本次仅修改前端展示和说明，未重新执行 Python 全量测试。

## 0.2.5 内在状态与行动驱动

- 新增独立内在状态模块，程序管理寂寞值和精力，在线连续增长、日程真实行动开始扣减；支持独立人工设值、增长及消耗配置、完整覆盖 0—100 的阶段编辑。离线及关闭模块不增长，每天不重置；开始前跳过不扣、失败不退，数值不足仍可执行并归零。
- 只有活动细化获得当前阶段文案；插件控制的模型请求不再投影精力数值或内在机制，细化不接受 `energy_delta`。删除三类每日活动上限及预留，保留每个对象发送限制、实际结果与防重。手动来源、试跑、回复、插话、天气及独立日报不扣值。
- 升级保留当前精力并初始化寂寞值，旧未来细化完整归档后清空，保留私人场合大纲；已开始活动及执行证据保留，历史不追扣。模板备份并启用新默认，重复迁移及旧备份不会覆盖新修改、重置扣减记录或复活旧版本。
- **401 项自动化通过，12 项跳过**。新增 58 项覆盖固定时钟与在线保存、阶段边界、扣值时机、多对象一轮、失败及低值、事务回滚、并发、防重凭据、跨午夜与私人场合统计、旧模板和备份迁移、未完成发布及已升级旧备份退役。12 项跳过仍来自缺少校验过的 AstrBot 4.27.5 官方源码缓存。
- **Ruff、JavaScript 语法检查、差异检查和完整页面冒烟通过**。检查了 1600px 桌面与 390px 手机截图，无横向溢出；覆盖阶段增删与连续范围、当前值与参数独立保存、失败保留、切换及刷新保留草稿与焦点、安全文本渲染、跨日统计，以及原有四项 JSON／SSE 调试视图。
- 当前本地环境为 Python 3.12.14 / AstrBot 4.28.0，模型和外部传输使用替身，HTTP 原文回归使用本地测试服务。本次没有真实 QQ 发送、在线模型或外部账号联调，也未部署、公开发布或上架；测试安装包仅含源码、页面、依赖声明和文档，不含测试截图及运行数据。

下列为历史版本的验收记录，其中每日活动上限、额度预留和模型控制精力的行为已由 0.2.5 替换。

## 0.2.4 日程大纲、活动细化与每日上限

- 正式生成只校验活动大纲；三类行动由细化决定，每活动每类最多一次，不强行凑满每日上限。公共模板与同次动态资料、本次要求分别维护；正式调用和隔离试跑共用资料组装。
- 预留、实际使用及成功／失败／跳过分开记录。上限即时生效，减少时优先保留较早的预留，增加时不恢复已取消行动；重新生成、重启和旧备份恢复不清零已用次数。按角色时区的实际开始日期计算跨午夜行动。
- 新闻／搜索通过能力检查后才开始计数；聊天在最终限制检查通过、首次生成消息前计一轮，多对象不重复扣次。已开始后失败或中断不退还，执行前被拦截释放预留。
- 手动细化和重新细化可填写本次要求；成功才替换，失败保留旧结果。编辑大纲归档并作废旧细化，自动细化最多两次，间隔至少 60 秒。生成跨越活动开始或结束、场合改变及并发编辑时不采用过期结果，不补做行动或虚构已过期经历。
- 同一 SQLite 事务内处理旧数据转换、细化替换与上限／预留调整。旧未开始活动改为待细化大纲，旧独立行动退出队列；已开始及已结束活动保留结果。旧模板可查看，清除了旧固定标记配额与旧执行分支，保留导入转换和历史展示。
- **343 项自动化通过，12 项跳过**。覆盖真实 SQLite 回滚、重复升级、旧备份恢复、异步检查期间编辑、跨版本结果去重、私有场合隔离、实际来源／社交调用起点及试跑无业务副作用。外部模型、消息传输和来源使用替身；HTTP 捕获专项继续使用本地测试服务。
- **Ruff、JavaScript 语法检查和完整页面冒烟通过**。1600px 桌面及 390px 手机截图已检查，无横向溢出；覆盖独立上限、细化弹窗、单次要求不复用、失败保留、决定理由、历史、旧模板及已有四项 JSON／SSE 调试功能。
- 用户已升级 AstrBot 4.28.0。本地测试的 `PYTHONPATH` 指向 `D:\AI\AstrBot`，实际导入源码 `astrbot.__version__` 与项目声明均为 **4.28.0**，Python 为 **3.12.14**；虚拟环境发行元数据仍显示 `4.28.0b1`，本次未改写宿主安装。12 项跳过来自缺少校验过的 4.27.5 官方源码缓存，不属于 4.28.0 运行测试失败。
- 本次未向真实 QQ 或在线模型发送测试，未部署、公开发布或上架。安装包仅含源码、页面、依赖声明和文档。

## 2026-09-09 格式化 JSON 层级折叠

- ②③格式化视图中的非空对象、数组默认全部展开，支持左侧箭头及“全部展开”“全部收起”；空容器和字符串中的括号不参与折叠。保留正文换行、字段顺序、重复字段和数字写法，无新增依赖或后端改动。
- 折叠状态按记录、请求及收发方向分别保留，切换视图或原文模式可恢复；父层收起保留子层状态，正文变化时重新展开。SSE 每个完整事件独立折叠，元数据和边界保留，未结束或无效内容继续显示原文。
- JavaScript 语法检查与完整页面冒烟通过。在原有 14 组 JSON/SSE 样例上增加结构识别、空容器、字符串括号、父子层状态、请求与返回隔离、正文变化重置、键盘与手机操作检查；折叠状态下复制下载仍与完整原文逐字一致。
- 无头 Edge 的 1600px 桌面、390px 手机展开及收起截图已检查，箭头位于左侧空白栏，正文和页面无横向溢出。页面及模型使用替身，沿用实际后端视图 fixture；本次为前端验证，不增加真实模型或 QQ 联调结论。

## 2026-09-09 手动重新生成今天日程

- 原「补生成缺失日程」替换为「重新生成日程」，使用操作开始时已保存的参数和模板；仅支持今天，有无旧计划及是否到默认生成时间均不影响手动生成。自动生成规则保持。
- 生成期间与日程推进、细化和编辑互斥，等待已有执行完成；合格后在一次 SQLite 事务内归档旧计划、替换当前计划并保留旧活动退役标记。失败、取消、跨日和模块变化不覆盖旧日程，新版本只执行采用时尚未到时的标记，已有发送限制不重置。
- 正式档案增加历史版本与替换前实际执行记录，关联调试显示历史采用结果；私人场合调整按版本重新应用，旧备份恢复不会重新激活退役活动。无数据库表结构迁移。
- 新增 21 项使用真实 SQLite 的重生成测试及 1 项管理 API／调试关联测试，覆盖参数与模板快照、连续重生成、生成失败、并发、取消、事务回滚、采用时间、新闻→搜索→聊天执行、场合隔离、重启和旧备份恢复。全量结果 **288 项通过、12 项跳过**；跳过均因当前未缓存 AstrBot 4.27.5 官方兼容源码，不记为通过。
- Ruff、JavaScript 语法检查和页面冒烟通过；1600px 桌面及 390px 手机截图检查无横向溢出，覆盖确认弹窗、未保存参数提示、日期限制、生成中禁用状态与历史折叠。顺带修正同一时间戳下记录排序及父子调试记录分组的稳定性，并用固定时钟回归。
- 环境为本地 Python 3.12.14 / AstrBot 4.28.0-beta.1，页面桥接、模型和发送使用替身；本次没有新增真实 QQ 或在线模型联调结论，也未公开发布或部署。

## 2026-09-09 调试页面可读性优化

- 保留四按钮切换，①的来源改为默认展开的单列折叠卡片；分别收起后仍显示出处与注入位置，切换视图或请求保留状态。
- ②③默认格式化 JSON 缩进与字符串换行，支持切换原文；SSE 逐个完整事件格式化，错误文本及未结束事件回退原文。复制、下载继续保留原始正文，后端捕获与存储未改动。
- JavaScript 语法检查、`git diff --check` 和完整页面冒烟测试通过。新增 14 组 JSON/SSE 格式化样例，覆盖多段中文、反斜杠、重复字段、数字精度与写法、事件边界和异常内容；同时验证来源折叠、键盘操作、请求配对、原文切换与复制下载一致性。
- 使用 Python 3.12.14、Node.js 24.19.0 和无头 Edge，在 1600px 桌面与 390px 手机检查来源及 JSON 截图；手机 JSON 字号提高至 12px，无页面或阅读框横向溢出。页面桥接与模型仍用替身，后端视图沿用实际代码生成的 fixture；本次未重新执行 Python 全量测试，不增加真实在线联调验收结论。

## 0.2.3 本轮交付与最终验证

- 四项调试页面、请求选择器、直接 JSON/SSE 正文下载、中文生活上下文、同次来源清单及设置入口 02 已完成。升级保留配置、日程、记忆、历史和执行记录。
- **278 项自动化通过，无跳过**。其中 25 项 HTTP 专项使用真实 AstrBot Provider、OpenAI SDK 3.8.0 和 aiohttp 本地 HTTP 服务；直接对比服务端收到的请求正文与导出结果、服务端发出的正文与记录结果。未知字段和普通同名数据保留，只隐藏已知认证值。
- HTTP 专项覆盖 Chat Completions / DeepSeek 兼容配置、Responses、SDK 重试、工具后第二次请求、并发及第三方内部调用排除、JSON/SSE、非 JSON 错误、异常返回字段、取消、客户端替换和卸载。流式正文出现字面 `[DONE]` 不冒充结束；失败和不完整事件分别标记。阅读版解析失败不影响原文保存。流中关闭调试后立即保留部分原文，重新开启不补收或伪称旧流完整，宿主仍收到完整回复。
- 用“莉莉现在日程是什么？”贯通真实消息钩子、宿主 Runner、Provider、SDK 与本地 HTTP，检查实际请求含当前活动、当天日程及中文资料，不含新增 UUID/历史统计或其他私聊材料；原有历史恢复与场合隔离继续回归。
- 其余新增验证覆盖中文投影和来源一致性、后台完整调用保留与清理、直接原文导出、旧记录不冒充原文、正式日程采用结果按生成记录关联，以及试跑不写正式数据。
- **Ruff 与 JavaScript 语法检查通过**。无头 Edge 在 1600px 桌面和 390px 手机通过页面验证并检查截图；使用实际后端 `build_views` 生成的 fixture，覆盖四项视图、收发配对、下载逐字一致、SSE、旧版和未适配状态、工具/采用/发送展示、试跑与模板分离、导航、日程归档入口及 XSS。页面桥接仍使用替身，不等于真实 AstrBot WebUI 联调。

### 4.27.5 专项边界

继续验证官方 4.27.5 人格解析函数；本轮另加载该版本完整 Chat Completions 与 Responses Provider 类，移除全局注册装饰器，Responses 继承加载的 4.27.5 基类，分别通过 JSON 与 SSE 共 4 项本地 HTTP 测试。其余宿主依赖来自本地 4.28.0-beta.1、Python 3.12.14 和 SDK 3.8.0，**不能称为完整 4.27.5 安装环境或在线联调**。

缓存通过 `python scripts/fetch_compat_source.py` 获取，均校验固定 SHA-256；缓存缺失时相关测试明确跳过，不计为通过。缓存不进入安装包。

| 官方来源 | SHA-256 |
|---|---|
| [4.27.5 人格解析](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/persona_mgr.py) | `14bfc9413f37ce3ab09e1a11233525874929b6707dcd41ee4c554f3b83633606` |
| [4.27.5 Chat Completions](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/provider/sources/openai_source.py) | `cd648baf5ab92357e3cb08326dcf4313662906f1e2d04959934762a299b72d13` |
| [4.27.5 Responses](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/provider/sources/openai_responses_source.py) | `78a0a2e6af3fb3d8c0e5c075bae9a578cc74733421da9e5886112fdf4dfc8a6d` |

真实 DeepSeek、QQ、天气和 B 站认证信息未提供，本轮没有向在线模型或真实 QQ 发送测试。下方真实联调待办继续保留。

## 0.2.2 历史交付

0.2.2 修复用户报告的“已有 24 条私聊历史，但人格未解析而无法接入”问题：传入真实 UMO 的宿主模型设置，统一配置检查和实际执行的判断；新增明确错误原因、人格来源和实际注入记录。聊天携带当前时间、当前活动及按场合过滤的当天日程摘要。

此前 0.2.1 的人格测试替身固定返回可用人格，源码兼容检查也没有验证默认人格参数的语义，因此漏掉了这个错误。本版已替换该测试方式，不能将此前的 223 项通过解释为已经验证了此场景。

以下为继续保留的 0.2.1 能力：

0.2.1 由主会话完成聊天上下文、会话只读检查、Provider 调用适配及整轮页面记录；未另开并行子智能体。本轮新增：

- 私聊当前对话接续、首次联系、成功发送后的宿主会话存档、动态资料不写入历史。
- 独立群观察、成员和引用信息、24 条／12,000 字符限制、人格隔离、重复群历史移除与保存前恢复，接管失败回退。
- 白名单首次对话／找到历史／读取失败显示；真实会话与人格诊断，不创建空会话。
- 按轮次捕获模型→工具→模型→发送，流式、失败、取消、并发、发送部分完成及卸载恢复；调试写入失败不影响业务。
- 聊天整轮保留、旧日志兼容、消息正文修复、单步和整轮 JSON、原始返回与解析字段分开。

以下为 0.2.0 已交付并继续保留的能力：

| 分工 | 已交付内容 |
|---|---|
| 主程 | 配置迁移、调试与隔离试跑、模板、宿主接入、白名单匹配、统一调用、集成审查与打包 |
| 日程 | 唯一正式日程、10/2/2/3 精确校验、标记重叠、顺序执行、未来活动调整、原始生成档案和恢复 |
| 来源 | 六个默认新闻源、新闻选择与正文读取、宿主搜索、和风天气、固定 B 站、独立日报及失败审计 |
| 页面 | 角色状态首页、独立白名单/来源/调试入口、明确按钮、完整 JSON、试跑与模板、桌面和手机布局 |

原有开发安排允许主会话统筹最多三个子智能体，并由主程审查集成。

## 0.2.2 历史自动化结果

- 环境：独立 **Python 3.12.14**，本地 **AstrBot 4.28.0-beta.1 / 4.28.0b1**。
- 最终集成测试：`python -m pytest -q` **244 项通过、无跳过**（0.2.1 为 223 项）。使用真实宿主请求、`ToolLoopAgentRunner`、消息类型和工具执行结构；模型、会话持久化服务、消息传输与来源使用替身。
- 新增 21 项验证：4.27.5 官方人格解析函数和本地 4.28 解析函数分别覆盖默认、当前对话、会话规则、禁用、未配置、缺失及不匹配；并分别贯通真实插件消息钩子→当前历史→日程组装→Provider 参数。另测连接、平台、读取、模块错误和缺失／关闭日程。
- 精确重现 4.27.5 漏传 `provider_settings` 返回空人格；同一已有 24 条历史的会话，修复后的检查与执行均通过。实际 Provider 请求包含测试日程、当前活动和当前时间，不含其他私聊的日程与调整；未改写已有历史。
- 本轮覆盖首次私聊、当前对话切换、首次主动消息存档、群观察和历史恢复、人格隔离、工具后模型调用、第三方内部调用排除、并发隔离、流式结果、取消、部分发送、关闭调试与卸载、完整轮次保留及调试写入失败隔离。
- 新增后台 Provider 参数校验、历史被其他链路修改时的回退，以及生成前／生成中卸载不丢失历史的回归。修正日程重试冷却测试的时钟控制，消除真实时钟产生的毫秒偏差。
- Ruff、Python 语法和 JavaScript 语法检查通过。
- 无头 Edge 页面测试通过，覆盖 **1600px 桌面及 390px 手机宽度**：白名单首次／历史／读取错误和只读检查，完整聊天轮次、两次模型及工具步骤、真实发送正文、整轮 JSON 和类别筛选，以及原有导航、配置、时间线、试跑、模板和记录管理。已检查白名单和调试页面截图。页面桥接使用替身，不能等同真实宿主 WebUI 联调。
- 页面新增验证配置允许与实际注入区分、默认人格来源、实际注入时间、跳转对应轮次，以及导航后检查状态保持；检查不调用模型或发送 QQ。
- 来源专项覆盖 RSS 失败隔离与正文提取、搜索空结果、天气认证/刷新/退避、B 站搜索与观看区分、公开视频记忆、日报作者日期、防重和重启。
- 回归覆盖默认配额、重叠、局部修改、已执行记录保留、模块热关闭、旧日程迁移、场合隔离、加权抽选、调试保留数量和试跑无业务副作用。
- 完整流程替身测试覆盖新闻候选→新闻事实→搜索事实→QQ 消息，验证新见闻参与表达、私人资料隔离、重启防重与关闭模块；首次打开页面即可获得完整默认任务模板。
- 延迟回归覆盖模型细化跨活动结束、新闻耗时后后续行动过期、执行窗口超时取消与防重记录保留。

宿主 `audioop` 弃用提醒属于 Python 3.12 宿主依赖提示，不影响当前测试。主程已完成整合检查、桌面与手机页面检查及源码安装包构建检查。

## 0.2.2 历史兼容核对

本轮除源码核对外，**实际执行了 v4.27.5 官方 `resolve_selected_persona` 函数**。源码只缓存于 `dist/compat`，不进入代码提交或安装包，校验 SHA-256 为 `14bfc9413f37ce3ab09e1a11233525874929b6707dcd41ee4c554f3b83633606`。测试只提取该函数进入隔离命名空间，用测试会话与设置驱动；这验证了真实解析逻辑，不等于安装完整 4.27.5 宿主后完成 QQ 联调。

复现命令：先运行 `python scripts/fetch_compat_source.py` 缓存固定来源，再运行 `python -m pytest -q`。没有缓存时 4.27.5 的 8 项测试会明确跳过，不能记为通过。已缓存后测试不需要网络。

安装声明为 `>=4.27.5,<5`。已只读核对官方 **v4.27.5 发布标签源码**：

| 接口 | 结论与依据 |
|---|---|
| Plugin Pages 请求与响应 | `request.body()`、`request.json(default)`、`json_response`、`error_response` 均存在；见 [api/web.py](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/api/web.py)。 |
| 路由、模型与提供商 | `register_web_api`、`registered_web_apis`、同步 `get_provider_by_id`、`llm_generate` 的上下文/系统提示词/附加参数匹配；见 [Context](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/star/context.py)。 |
| 模型名称覆盖 | OpenAI Provider 支持 `model` 参数；见 [openai_source.py](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/provider/sources/openai_source.py)。 |
| 发送后记录 | `after_message_sent` 已导出并注册对应事件；见 [filter 导出](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/api/event/filter/__init__.py)和[注册实现](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/star/register/star_handler.py)。 |
| 网页搜索 | 宿主搜索设置、六种服务选择和内置工具查找匹配；见 [主 Agent](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/astr_main_agent.py)及[工具管理器](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/provider/func_tool_manager.py)。 |
| Agent 生命周期及工具钩子 | `on_agent_begin`、`on_agent_done`、工具前后事件可用；见 [astr_agent_hooks.py](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/astr_agent_hooks.py)。 |
| 会话默认人格 | `provider_settings.default_personality` 必须传给解析器；已执行官方函数并验证优先级和失败分支，见 [persona_mgr.py](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/persona_mgr.py)。 |
| 逐次模型捕获 | `step`、`_iter_llm_responses`、Provider 调用及 Agent 开始／结束入口存在；见 [tool_loop_agent_runner.py](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/agent/runners/tool_loop_agent_runner.py)。 |
| 动态资料不入存档 | `TextPart`、`_no_save`、`dump_messages_with_checkpoints` 和 `bind_checkpoint_messages` 存在；见 [message.py](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/agent/message.py)。 |
| 主动消息接续会话 | `session_lock_manager.acquire_lock`、`new_conversation` 的人格参数及 `update_conversation` 存在；见 [会话锁](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/utils/session_lock.py)及[会话管理器](https://raw.githubusercontent.com/AstrBotDevs/AstrBot/v4.27.5/astrbot/core/conversation_mgr.py)。 |

这些是源码兼容核对，**没有安装 4.27.5 实机完成在线模型、QQ 或来源验收**。不能把 4.28 自动化结果写成 4.27.5 实测。

## 真实联调待办

本轮没有用于实际联调的模型、QQ、和风认证或 B 站账号凭据，下列项目不记为已通过：

- [ ] 在目标 AstrBot 4.27.5 安装/升级、打开 Plugin Pages、保存配置并重载。
- [ ] 用真实模型生成一次合格的 10 活动大纲，验证细化只收到阶段文案、三类行动无每日上限、未来活动调整及前一日约定影响。
- [ ] 使用明确测试白名单完成群聊/私聊主动消息、上下文读取、群聊插话、免打扰和频率控制。
- [ ] 真实账号首次联系及后续回复接续、切换对话、群成员近期消息与引用；核对原始宿主历史保留。
- [ ] 真实一轮工具调用后的第二次模型、流式发送、并发和传输失败，在调试页四项视图对照；确认未接入原因显示。
- [ ] 在不同群与私聊确认人物识别及具体谈话、约定、日程衍生内容不串场合。
- [ ] 获取真实 RSS 正文、宿主搜索、和风实时天气、B 站搜索与观看结果。
- [ ] 在各自定时点读取两路 AI 日报，核对作者、当日发布日期、缓存标记和独立次数。
- [ ] 下载真实请求 JSON 核对，再在任务试跑编辑器调整输入，确认只有模型调用，没有工具执行、QQ 发送或正式数据变更。
- [ ] 在真实运行中关闭模块、改变配置、模拟来源失败、重载和重启，验证不重复或集中补发。
- [ ] 恢复备份，确认旧 ID、防重记录保留，所有业务模块关闭，检查后再启用。

## 实际边界

- 轻量中文文本检索，无向量数据库；日程、提炼和感想的质量取决于真实模型，长期自然程度待使用验证。
- 新闻源可逐项修改；天气固定和风当前接口，搜索使用宿主配置，B 站依赖固定插件及公开 API v3。天气成功刷新间隔 90 分钟，失败 15 分钟退避，不调用 AI。
- 新闻全文不可读时只使用实际标题或订阅摘要，并标明依据；模型感想失败保留实际证据且标记部分完成，不编造成功。
- 日报无法核对作者或日期时跳过，旧视频记忆不标为新观看；独立日报不扣内在状态，不增加日程聊天。
- 调试②③保存 DeepSeek/OpenAI 兼容 Chat Completions 与 Responses 的实际 HTTP 正文，包含 SDK 的实际 HTTP 重试；SSE 保存原始事件流，阅读版另行合并。未适配或没有原文明确标注，旧 Provider 参数不能替代原文；第三方内部调用不捕获。只隐藏已知认证凭据。特殊媒体/工具上下文未支持直接试跑的字段明确拒绝。
- 发送成功表示传输调用返回，不代表对方已读；传输异常可能已发出部分内容，明确标为未知／部分完成。主动私聊存档失败另行记录，不自动重发。
- 升级不重建当天日程，恢复采用缺失记录合并并关闭全部模块；清理调试不删正式档案或防重记录。
- 管理员视图允许看全部场合，实际聊天仍过滤；核心人格不自动改写，不含世界居民、梦境或复杂技能成长。

## 安装包

运行 `python scripts/build_test_package.py` 生成 `dist/astrbot_plugin_living_world-0.2.5.zip` 及 SHA-256 文件。脚本检查入口文件、ZIP 完整性及包内 Python 语法；仅包含源码、页面、依赖声明、许可证和文档，不包含环境、数据库、兼容测试源码缓存、页面截图及凭据。

公开发布和插件市场上架另行安排。
